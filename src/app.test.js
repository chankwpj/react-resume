import { shallow } from 'enzyme';
import App from './App';

it("render loading page given fetch is loading", () => {

});

it("render error page given fetch returns erro", () => {

});

it("render page given fetch returns data", () => {

});
